def sum_of_digits(number):
    
    digit_sum = 0
    
    
    while number > 0:
       
        digit_sum += number % 10
        
        number //= 10
    
    return digit_sum


number = 12345
print("Sum of digits of", number, "is:", sum_of_digits(number))
